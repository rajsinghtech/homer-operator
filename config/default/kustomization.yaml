apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: homer-operator-system
namePrefix: homer-operator-

resources:
- ../crd
- ../rbac
- ../manager
# Keep ServiceMonitor out of the default install. The optional
# config/prometheus overlay requires the Prometheus Operator CRD.
